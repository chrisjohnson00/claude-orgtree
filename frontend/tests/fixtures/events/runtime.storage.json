{
  "variant": "runtime.storage",
  "family": "runtime_recovery",
  "private": {
    "v": 1,
    "variant": "runtime.storage",
    "actor": {
      "kind": "system",
      "id": "@system"
    },
    "object": {
      "kind": "org",
      "org": "runtime.storage·object.org"
    },
    "engine_authored": true,
    "level": "over",
    "used_mb": 920.0,
    "cap_mb": 1000.0,
    "scope": "storage"
  },
  "public": {
    "v": 1,
    "variant": "runtime.storage",
    "projection": "public",
    "actor": {
      "kind": "system",
      "id": "@system"
    },
    "object": {
      "kind": "org"
    },
    "level": "over",
    "used_mb": 920.0,
    "cap_mb": 1000.0,
    "scope": "storage"
  },
  "body": "⚠ The org is OVER its storage limit (920.0 / 1000 MB — workspace + scratch + uploads together). File creation and writes in the workspace and every scratch folder are now BLOCKED at the OS level — new writes will fail with permission errors. Deleting still works: remove large files you created and the block lifts automatically at the next check. Do NOT keep generating files."
}
